<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- =========================================== -->
    <!-- Kế thừa từ Root Parent POM của dự án        -->
    <!-- =========================================== -->
    <parent>
        <groupId>com.cns.lg</groupId>
        <artifactId>uber-app</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>ride-service</artifactId>
    <name>ride-service</name>
    <description>
        Ride Service - Quản lý vòng đời toàn bộ chuyến đi (Ride lifecycle).
        Xử lý: tạo yêu cầu đặt xe → chờ ghép cặp → xác nhận tài xế → đang đến đón
        → đang di chuyển → hoàn thành/huỷ chuyến đi.
        Lưu trữ bền vững vào MySQL qua JPA/Hibernate, giao tiếp async qua Kafka.
    </description>

    <!-- =========================================== -->
    <!-- Dependencies riêng của ride-service         -->
    <!-- (version kế thừa từ Root POM / Spring BOM)  -->
    <!-- =========================================== -->
    <dependencies>

        <!-- Spring Web MVC: REST API cho client (Rider/Driver app) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Data JPA: ORM layer - thao tác MySQL (Hibernate) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- Spring Kafka: Producer (phát ride-request-event) và Consumer (nhận match-found-event) -->
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
        </dependency>

        <!-- Spring Validation: Validate request body từ API -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Actuator: Health check & metrics endpoint -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- MySQL Connector: JDBC driver kết nối MySQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Lombok: Giảm boilerplate code (getter/setter/builder/log...) -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- DevTools: Hot reload khi phát triển -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>

        <!-- ===== Test Dependencies ===== -->

        <!-- spring-boot-starter-test: JUnit 5, Mockito, AssertJ, MockMvc -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Kafka Test: EmbeddedKafka cho integration test -->
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <!-- =========================================== -->
    <!-- Build: Kích hoạt plugin từ Root pluginMgmt  -->
    <!-- =========================================== -->
    <build>
        <plugins>
            <!-- Đóng gói thành Executable Fat JAR -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <!-- Compiler với Lombok annotation processor -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
